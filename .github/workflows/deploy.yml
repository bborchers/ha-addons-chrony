name: Deploy

on:
  release:
    types: [published]

jobs:
  deploy:
    uses: bborchers/ha-addons-workflow/.github/workflows/build-deploy.yml@main
    with:
      addon-path: chrony_timeserver
      slug: chrony_timeserver
    secrets:
      dispatch-token: ${{ secrets.DISPATCH_TOKEN }}
    permissions:
      contents: read
      packages: write
